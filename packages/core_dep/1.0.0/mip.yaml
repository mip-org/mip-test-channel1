name: core_dep
description: "Test package depending on chebfun (fully qualified) in the core channel"
version: "1.0.0"
license: MIT
dependencies: [mip-org/core/chebfun]

paths:
  - path: "."

builds:
  - architectures: [any]
